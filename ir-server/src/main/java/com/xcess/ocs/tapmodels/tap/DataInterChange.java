/*
 * This class file was automatically generated by ASN1bean v (http://www.beanit.com)
 */

package com.xcess.ocs.tapmodels.tap;

import java.io.IOException;
import java.io.EOFException;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.ByteArrayOutputStream;
import java.util.List;
import java.util.ArrayList;
import java.util.Iterator;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import java.io.Serializable;
import com.beanit.asn1bean.ber.*;
import com.beanit.asn1bean.ber.types.*;
import com.beanit.asn1bean.ber.types.string.*;


public class DataInterChange implements BerType, Serializable {

	private static final long serialVersionUID = 1L;

	private byte[] code = null;
	private TransferBatch transferBatch = null;
	private Notification notification = null;
	
	public DataInterChange() {
	}

	public DataInterChange(byte[] code) {
		this.code = code;
	}

	public void setTransferBatch(TransferBatch transferBatch) {
		this.transferBatch = transferBatch;
	}

	public TransferBatch getTransferBatch() {
		return transferBatch;
	}

	public void setNotification(Notification notification) {
		this.notification = notification;
	}

	public Notification getNotification() {
		return notification;
	}

	@Override public int encode(OutputStream reverseOS) throws IOException {

		if (code != null) {
			reverseOS.write(code);
			return code.length;
		}

		int codeLength = 0;
		if (notification != null) {
			codeLength += notification.encode(reverseOS, true);
			return codeLength;
		}
		
		if (transferBatch != null) {
			codeLength += transferBatch.encode(reverseOS, true);
			return codeLength;
		}
		
		throw new IOException("Error encoding CHOICE: No element of CHOICE was selected.");
	}

	@Override public int decode(InputStream is) throws IOException {
		return decode(is, null);
	}

	public int decode(InputStream is, BerTag berTag) throws IOException {

		int tlvByteCount = 0;
		boolean tagWasPassed = (berTag != null);

		if (berTag == null) {
			berTag = new BerTag();
			tlvByteCount += berTag.decode(is);
		}

		if (berTag.equals(TransferBatch.tag)) {
			transferBatch = new TransferBatch();
			tlvByteCount += transferBatch.decode(is, false);
			return tlvByteCount;
		}

		if (berTag.equals(Notification.tag)) {
			notification = new Notification();
			tlvByteCount += notification.decode(is, false);
			return tlvByteCount;
		}

		if (tagWasPassed) {
			return 0;
		}

		throw new IOException("Error decoding CHOICE: Tag " + berTag + " matched to no item.");
	}

	public void encodeAndSave(int encodingSizeGuess) throws IOException {
		ReverseByteArrayOutputStream reverseOS = new ReverseByteArrayOutputStream(encodingSizeGuess);
		encode(reverseOS);
		code = reverseOS.getArray();
	}

	@Override public String toString() {
		StringBuilder sb = new StringBuilder();
		appendAsString(sb, 0);
		return sb.toString();
	}

	public void appendAsString(StringBuilder sb, int indentLevel) {

		if (transferBatch != null) {
			sb.append("transferBatch: ");
			transferBatch.appendAsString(sb, indentLevel + 1);
			return;
		}

		if (notification != null) {
			sb.append("notification: ");
			notification.appendAsString(sb, indentLevel + 1);
			return;
		}

		sb.append("<none>");
	}

}

